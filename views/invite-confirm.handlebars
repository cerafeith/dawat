<article class="container mt-2">
    <h1 class="bd-title">Confirmation</h1>
    <p class="lead">Are you sure you want to join <strong>{{group.name}}</strong>?</p>
    <form action="/invite/{{group.id}}" method="POST">
        <button class="btn btn-primary btn-sm" type="submit">Continue</button>
    </form>
    
</article>